'use client';

import React from 'react';
import ChatWidget from './ChatWidget';

// This component wraps the chat widget
export const ChatContainer: React.FC = () => {
  return <ChatWidget />;
};

export default ChatContainer; 